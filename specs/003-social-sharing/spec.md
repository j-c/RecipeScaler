# 003 — Social Sharing (Deferred)

## Background

The `ng2-sharebuttons` package was removed during the Angular 2 → 19 upgrade
(spec `001-modernize-dependencies`) because it is unmaintained and incompatible
with modern Angular.

## Scope

Re-implement social-sharing functionality (FR-014) using a maintained library
or native Web Share API.

## Status

**Placeholder** — not yet specified. Run `/speckit.spec` when ready to begin.
